name: Build & Push Image

env:
  REGISTRY: "quay.io/r0x0d"
  TAGS: latest ${{ github.sha }}

on:
  workflow_call:
    inputs:
      matrix:
        required: true
        type: string
    secrets:
      BOT_USERNAME:
        required: true
      BOT_SECRET:
        required: true

jobs:
  build:
    runs-on: ubuntu-24.04
    container:
      image: quay.io/travier/podman-action@sha256:505aa1f7564ffe3eb76f6729e30e6849a85463a3088f348e23386bdf7a938e83
      options: "--security-opt=label=disable --privileged --user 0:0 --device=/dev/kvm --device=/dev/fuse --volume /:/run/host:rw"
    steps:
      - name: Setup Docker config dir
        run: mkdir -p /github/home/.docker/

      - name: Reclaim disk space
        run: rm -rf "/run/host/usr/local/lib/android"

      - name: Checkout repo
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt install -y jq

      - name: Build image
        uses: redhat-actions/buildah-build@7a95fa7ee0f02d552a32753e7414641a04307056 # v2
        with:
          context: .
          image: toolbox-${{ matrix.image }}
          tags: ${{ env.TAGS }}
          containerfiles: toolbox/${{ matrix.container_file }}
          layers: false
          oci: true

      - name: Push image
        uses: redhat-actions/push-to-registry@5ed88d269cf581ea9ef6dd6806d01562096bee9c # v2
        id: push
        if: ${{ github.event_name == 'push' || github.event_name == 'schedule' }}
        with:
          username: ${{ secrets.BOT_USERNAME }}
          password: ${{ secrets.BOT_SECRET }}
          image: ${{ matrix.image }}
          registry: ${{ env.REGISTRY }}
          tags: ${{ env.TAGS }}
          extra-args: |
            --compression-format=zstd:chunked
            --compression-level=19

      - name: Login to registry
        uses: redhat-actions/podman-login@4934294ad0449894bcd1e9f191899d7292469603 # v1
        if: ${{ github.event_name == 'push' || github.event_name == 'schedule' }}
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.BOT_USERNAME }}
          password: ${{ secrets.BOT_SECRET }}
          auth_file_path: /tmp/auth.json

      - uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac # v3.9.1
        if: ${{ github.event_name == 'push' || github.event_name == 'schedule' }}

      - name: Sign image (keyless)
        if: ${{ github.event_name == 'push' || github.event_name == 'schedule' }}
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: |
          cosign sign -y quay.io/r0x0d/${{ matrix.image }}@${{ steps.push.outputs.digest }}
