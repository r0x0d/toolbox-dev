---
- name: Install Zig
  ansible.builtin.dnf:
    name: zig
    state: present
  when: languages.zig_development | default(false) | bool

- name: Create wrapper scripts
  ansible.builtin.template:
    src: "{{ playbook_dir }}/templates/bin-wrapper.sh.j2"
    dest: "{{ tdx_user_bin }}/{{ item }}"
    owner: "{{ tdx_user }}"
    mode: "0755"
  loop: "{{ zig_development_binaries }}"
  when: languages.zig_development | default(false) | bool

- name: Remove wrapper scripts
  ansible.builtin.file:
    path: "{{ tdx_user_bin }}/{{ item }}"
    state: absent
  loop: "{{ zig_development_binaries }}"
  when: not (languages.zig_development | default(false) | bool)
